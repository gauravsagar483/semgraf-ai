# Graph / relationship plane

Phase 0–1: `ecommerce/lineage.json` drives in-process blast-radius / lineage.

Phase 2: map the same toy tables into a graph engine via Docker Compose profile `graph`.
Keep schemas fixture-only — never import employer graph definitions.

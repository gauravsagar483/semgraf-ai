---
description: Semgraf product scope — dual-plane MVP, surfaces, non-goals
alwaysApply: false
globs: "**/*"
---

# Product scope

Canonical plan: `.agents/plans/dual-plane_agent_framework_6af3daeb.plan.md`

## Build (Phase 1)

1. Dual paths on one toy world: metric compile + graph traverse
2. Thin own SQL compiler + OSI-derived YAML (not pinned Ossie 0.x)
3. PuppyGraph on toy tables for relationship plane
4. Hybrid router: rules for structural cues + LLM when ambiguous
5. **Premium web** Ask + Trace (hero) — Attio-grade craft, Semgraf brand-first
6. SDK: `semgraf` (Py) + `@semgraf/sdk` (TS); MCP same core
7. Fixtures: ecommerce primary; fintech pack second

## Non-goals v1

- Enterprise-search clone
- Datus-class agent OS / 11 DB adapters
- Public OSS core (closed product)
- Paperclip as product runtime or Week-0 foundation
- Blast-radius-only clone of dbt MCP
- 16-dialect Colrows parity
- Port of any employer internal product

## Positioning check

If pitch sounds like Alkera (agent) or Kaelio (context layer) → rewrite toward **runtime under agents: compile + traverse**.

---
name: locked-product-decisions
description: "Phase-1 Semgraf locks from grill — dual MCP paths, thin compiler, PuppyGraph, premium web, OSI-derived YAML."
metadata:
  node_type: memory
  type: project
---

Phase-1 locks (do not re-open unless founder asks):

| Decision | Lock |
|----------|------|
| Paths | Dual day one: `ask_metric` + `ask_graph` on **one toy world** |
| Compile | Thin **own** compiler now; MetricFlow/OSI later |
| Semantic YAML | **OSI-derived** (shapes like Ossie; not pinned 0.x) |
| Graph | **PuppyGraph** on toy tables |
| Surface | **Premium web** Ask+Trace hero (Attio craft / Colrows clarity; own brand) |
| Also | Custom SDK (Py+TS) + MCP; web dogfoods TS SDK |
| Router | Rules for obvious structural + **LLM hybrid** when ambiguous |
| Fixtures | Ecommerce primary + Fintech-lite second pack |
| Deploy shape | Closed product; hybrid CLI/cloud later — not public OSS core |
| Pace | Nights/weekends ~8–12h/wk |

**Non-goals v1:** enterprise-search clone, Datus-class agent OS, 11 DB adapters, blast-radius as only hero, Paperclip as product foundation, employer IP.

**Why:** YC needs one sharp dual-path demo; hybrid mega-stack = death. Colrows-shaped minimal.
**How to apply:** cut scope to these locks; sequence “all” asks into phases. Related: [[semgraf-identity]] [[founder-constraints]].

---
name: semgraf-identity
description: "Semgraf brand lock — dual-plane runtime; compile metrics + traverse relationships."
metadata:
  node_type: memory
  type: project
---

**Brand:** Semgraf (locked). Say “Sem-graf” (≠ Semantica).

**One-liner:** Semgraf — dual-plane runtime for data agents: compile governed metrics, traverse verified relationships.

**YC pitch:** metrics that compile, relationships that verify — one runtime under your agents.

**Are:** dual-plane product (metric compile ∥ relationship traverse). Premium web + SDK + MCP.

**Are not:** Alkera/Datus (agent), Kaelio/ktx (context layer), enterprise-search clone, employer-product port, Colrows feature-parity, Semantica lane.

**Packages:** `semgraf` / `@semgraf/sdk`

**Repo:** private `gauravsagar483/semgraf` (target). This repo `semgraf-ai` = plans/agent ops until scaffold.

**Why:** name rare vs Stratum/Rivet/Meridian/Twofold; meaning = semantic + graph.
**How to apply:** always brand Semgraf; never pitch as context layer / data agent / employer-lite. Related: [[locked-product-decisions]] [[competitors-usp]].

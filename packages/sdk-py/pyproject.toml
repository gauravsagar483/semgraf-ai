[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "semgraf"
version = "0.1.0"
description = "Semgraf Python SDK — dual-plane client"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]

[tool.hatch.build.targets.wheel]
packages = ["semgraf"]
